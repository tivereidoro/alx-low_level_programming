#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: added to m
 * @y: added to n
 * Return: returns result of addition of two integers
 */
int add(int x, int y)
{
	int result;

	result = x + y;

	return (result);
}
