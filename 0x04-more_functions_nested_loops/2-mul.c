#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @a: first variable
 * @b: second variable
 * Return: the product of both variables
 */
int mul(int a, int b)
{
	int a, b, result;

	result = a * b;
	return (result);
}
